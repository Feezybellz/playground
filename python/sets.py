# Set only display unique values so values can't appear more than once
# To create a set use the predefined function "set()" e.g below
_set = set()

# To add element or data to the set use the add methid e.g below
_set.add(1)
_set.add(3)
_set.add(7)
_set.add(4)
_set.add(9)

# Adding a value that existed before
_set.add(4)

# To remove a value from a set you can use the remove methid e.g below
_set.remove(3)

# To get the length of set you can use the predefined function "len()" e.g below




print(f"The set contains {len(_set)} elements")
